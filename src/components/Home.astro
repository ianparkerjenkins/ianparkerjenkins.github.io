---
import lon from '../assets/long-shot-bird-lake-with-blurred-background.jpg';
---

<div>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poiret+One&display=swap" rel="stylesheet">
    <div>


        <div class="grid">
            <div class="card intro">
                <h1>Hi! I’m Ian 👋.</h1>
                <!-- <h1></h1> -->
                <!-- <h1>I'm a software and machine learning engineer 🤖 with a background in physics ⚛️, and an artist 🎨. </h1> -->
                <h1>I like building things to explore ideas at the intersection of technology ⚙️ and creativity 💡; everything from code to models to visuals.</h1>
                <h1>In my free time, I enjoy drawing ✏️, traveling ✈️, hiking 🌱, making coffee ☕, and tinkering with side projects 🛠️.</h1>
            </div>
            <div class="card wide">
                <img class="card-image" src={lon.src} alt="Project 1" />
                <div class="card-text">
                    <p>Project 1</p>
                </div>
            </div>
            <div class="card">
                <img class="card-image" src={lon.src} alt="Project 1" />
                <div class="card-text">
                    <p>Project 2</p>
                </div>
            </div>
            <div class="card">Item 3</div>
            <div class="card">Item 4</div>
            <div class="card">Item 5</div>
            <div class="card wide">Item 6</div>
            <div class="card">Item 7</div>
            <div class="card">Item 8</div>

        </div>

    </div>
</div>

<style>
    
    h1 {
        font-family: "Poiret One", sans-serif;
        font-weight: 400;
        font-style: normal;
    }

    .grid {
        display: grid;
        /* grid-auto-rows: 300px; */
        grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
        gap: 1.2rem;
        padding: 2rem;
    }

    .card.tall {
        grid-row: span 2;
        aspect-ratio: auto; 
    }

    .card.wide {
        grid-column: span 2;
        aspect-ratio: auto;
    }

    .card.intro {
        background: #ffffff;
        grid-column: span 2;
        aspect-ratio: auto;
    }

    /* .card.big {
        background: #ffffff;
        grid-column: span 2;
        grid-row: span 2;
        aspect-ratio: auto;
    } */

    @media (max-width: 640px) {
        .card.tall,
        .card.wide,
        .card.big {
            grid-column: span 1;
            grid-row: span 1;
            aspect-ratio: 1 / 1;
        }
    }

    /* .card {
        background: #ededed;
        border-radius: 12px;
        padding: 1em;
        font-size: 1rem;
        aspect-ratio: 1 / 1; 
    } */

    .card {
        background: #ededed;
        position: relative;       /* allows absolute positioning of text */
        overflow: hidden;         /* prevents image from spilling */
        border-radius: 12px;
        cursor: pointer;
        font-size: 1.25rem;
        aspect-ratio: 1 / 1;      /* keeps square */
    }

    .card img {
        width: 100%;
        height: 100%;
        object-fit: cover;       /* fill card and crop if needed */
        display: block;
        transition: transform 0.3s ease;
    }

    .card:hover img {
        transform: scale(1.05);
    }

    .card-text {
        position: absolute;
        bottom: 0;               /* text appears at bottom */
        left: 0;
        right: 0;
        padding: 1rem;
        color: rgb(255, 255, 255);
        background: linear-gradient(to top, rgba(0,0,0,0.7), transparent);
        opacity: 0.2;
        transition: opacity 0.3s ease;
    }

    .card:hover .card-text {
        opacity: 1;
    }

</style>